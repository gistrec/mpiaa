#include "graph_algs.h"
#include "iostream"

/*
void main() {
	Graph graph;
	graph.add_vertex(0);
	graph.add_vertex(1);
	graph.add_vertex(2);
	graph.add_vertex(3);
	graph.add_vertex(4);
	graph.add_vertex(5);
	graph.add_edge(0, 1);
	graph.add_edge(2, 3);

	std::vector<std::vector<int>> result = connected_components(graph);

	graph.add_edge(0, 3);
	graph.add_edge(2, 4);
	graph.add_edge(2, 5);

	std::vector<int> path = shortest_path(graph, 0, 5);
	for (std::vector<int>::iterator verticle = path.begin(); verticle != path.end(); ++verticle) {
		std::cout << *verticle << " ";
	}
	system("pause");
}
*/